package com.jite.hibgen.dao;

import com.jite.hibgen.model.*;

import java.util.List;
import javax.naming.InitialContext;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.hibernate.LockMode;
import org.hibernate.SessionFactory;
import static org.hibernate.criterion.Example.create;

/**
 * Home object for domain model class TSystemRole.
 * @see com.jite.hibgen.dao.TSystemRole
 * @author Hibernate Tools
 */
public class TSystemRoleDao {

	private static final Log log = LogFactory.getLog(TSystemRoleDao.class);

	private final SessionFactory sessionFactory = getSessionFactory();

	protected SessionFactory getSessionFactory() {
		try {
			return (SessionFactory) new InitialContext()
					.lookup("SessionFactory");
		} catch (Exception e) {
			log.error("Could not locate SessionFactory in JNDI", e);
			throw new IllegalStateException(
					"Could not locate SessionFactory in JNDI");
		}
	}

	public void persist(TSystemRole transientInstance) {
		log.debug("persisting TSystemRole instance");
		try {
			sessionFactory.getCurrentSession().persist(transientInstance);
			log.debug("persist successful");
		} catch (RuntimeException re) {
			log.error("persist failed", re);
			throw re;
		}
	}

	public void attachDirty(TSystemRole instance) {
		log.debug("attaching dirty TSystemRole instance");
		try {
			sessionFactory.getCurrentSession().saveOrUpdate(instance);
			log.debug("attach successful");
		} catch (RuntimeException re) {
			log.error("attach failed", re);
			throw re;
		}
	}

	public void attachClean(TSystemRole instance) {
		log.debug("attaching clean TSystemRole instance");
		try {
			sessionFactory.getCurrentSession().lock(instance, LockMode.NONE);
			log.debug("attach successful");
		} catch (RuntimeException re) {
			log.error("attach failed", re);
			throw re;
		}
	}

	public void delete(TSystemRole persistentInstance) {
		log.debug("deleting TSystemRole instance");
		try {
			sessionFactory.getCurrentSession().delete(persistentInstance);
			log.debug("delete successful");
		} catch (RuntimeException re) {
			log.error("delete failed", re);
			throw re;
		}
	}

	public TSystemRole merge(TSystemRole detachedInstance) {
		log.debug("merging TSystemRole instance");
		try {
			TSystemRole result = (TSystemRole) sessionFactory
					.getCurrentSession().merge(detachedInstance);
			log.debug("merge successful");
			return result;
		} catch (RuntimeException re) {
			log.error("merge failed", re);
			throw re;
		}
	}

	public TSystemRole findById(java.lang.Long id) {
		log.debug("getting TSystemRole instance with id: " + id);
		try {
			TSystemRole instance = (TSystemRole) sessionFactory
					.getCurrentSession().get("com.jite.hibgen.dao.TSystemRole",
							id);
			if (instance == null) {
				log.debug("get successful, no instance found");
			} else {
				log.debug("get successful, instance found");
			}
			return instance;
		} catch (RuntimeException re) {
			log.error("get failed", re);
			throw re;
		}
	}

	public List<TSystemRole> findByExample(TSystemRole instance) {
		log.debug("finding TSystemRole instance by example");
		try {
			List<TSystemRole> results = (List<TSystemRole>) sessionFactory
					.getCurrentSession()
					.createCriteria("com.jite.hibgen.dao.TSystemRole")
					.add(create(instance)).list();
			log.debug("find by example successful, result size: "
					+ results.size());
			return results;
		} catch (RuntimeException re) {
			log.error("find by example failed", re);
			throw re;
		}
	}
}
